import 'palette.json';

class Palette{
  Color primary, secondary, background, action;
  Palette(this.primary, this.secondary, this.background, this.action)
}

static class ColorProvider {

  ColorProvider(){
  }

  const int paletteCount{
    return 
  }

  Color getColor(colorType type){

  }

  Enum colorType {primary, secondary, background, action}
}
